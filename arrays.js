// variable that stores aarays of strings

var cities = ["Denver", "Fort Collin", "Boulder", "Golden"];

//array of number

var num = [2, 4, 16, 32];

//arrays of booleans

var true_or_false = [age = false, car = false, job = true, name = true];

//push will add "spring" at the end of the array
cities.push("spring");

//will return an a array of 2 elemement of index 1,2
cities. silice(2,3);

// this will remove the last element from the array
cities.pop();
